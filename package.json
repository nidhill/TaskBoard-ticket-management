{
  "name": "taskboard-mern",
  "version": "1.0.0",
  "description": "TaskBoard - Modern Task Management System (MERN Stack)",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "keywords": [
    "mern",
    "taskboard",
    "project-management"
  ],
  "author": "TaskBoard",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}